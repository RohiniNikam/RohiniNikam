const object = {
  name: "Bard",
  age: 1,
  occupation: "Large Language Model",
  city: "Supa"
};

// Count the number of keys/properties in the object using the Object.keys() method and the length property
const numberOfKeys = Object.keys(object).length;

// Log the number of keys/properties to the console
console.log(numberOfKeys); // 3
