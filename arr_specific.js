const arr = [2, 5, 9, 6];

// Check if the array contains the value 5
const indexOfValue = arr.indexOf(5);

// Check if the value is found in the array by comparing the return value to -1
const containsValue = indexOfValue !== -1;

// Log the result
console.log(containsValue); // true
