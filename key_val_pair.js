const object = {};

// Add a key/value pair to the object using dot notation
object.name = "Bard";
object.age = 21;

// Check if the key/value pair was added to the object
console.log(object.age); // 21
console.log(object.name); // Bard

